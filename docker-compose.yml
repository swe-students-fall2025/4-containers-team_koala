version: "3.9"

services:
  ml:
    build: ./machine-learning-client
    devices:
      - "/dev/video0:/dev/video0"   # camera passthrough
    ports:
      - "8000:8000"

  web:
    build: ./web-app
    ports:
      - "5000:5000"
    depends_on:
      - ml
